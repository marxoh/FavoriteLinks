<div class="containder p-4">
  <div class="row">
    {{! recorrer links para mostrar en html }}
    {{#each links}}
      {{!-- {{this}} //muestra:  [object Object] --}}
      <div class="col-md-3">
        <div class="card text-center">
          <div class="card-body">
            <a href="{{url}}" target="_blank">
              <h3 class="card-title text-uppercase">{{title}}</h3>
            </a>
            <p class="m-2">{{description}}</p>
            {{! llama a un atributo de una funcion declarada en los helpers de exphbs, los parametros se ponen separados con espacio }}
            <p>{{timeago created_at}}</p>
            {{! parece que este id lo tiene manejado desde antes en todas las paginas }}
            <a href="/links/delete/{{id}}" class="btn btn-danger">Borrar link</a>
            <a href="" class="btn btn-secondary">Editar</a>
          </div>
        </div>
      </div>
    {{else}}
    <div class="col-md-4 mx-auto" >
        <div class="card card-body text-center">
          <p>No hay links creados aún.</p>
          <a href="/links/add">Crea uno!</a>
        </div>
    </div>
    {{/each}}
  </div>
</div>